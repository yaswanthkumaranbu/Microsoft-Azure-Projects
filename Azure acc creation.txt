Microsoft azure acc = yaswanthkumaranbu@gmail.com (password:Yaswanthkumar@123)
virtual machine : Yashvm (password:Yaswanthkumar@123)
To access vm (use ssh -p 22 Yashvm@(public IP))

(Day 1)
Task 1 : completed (vm creation using portal)
Task 2: completed (vm creation using terraform) (https://learn.microsoft.com/en-us/azure/virtual-machines/windows/quick-cluster-create-terraform?tabs=azure-cli)

vm creation using terraform

1.install azure cli
2.set path(where az)
3.install teraform in c (under a folder)  (https://developer.hashicorp.com/terraform/install?product_intent=terraform)
4.create terraform folder and save codes from (https://learn.microsoft.com/en-us/azure/virtual-machines/windows/quick-cluster-create-terraform?tabs=azure-cli)
5.cmds (az login -> terraform init -> terraform plan -> terraform apply)

(Day 2)  (registry.terraform.io)
Fornoon task : completed (Create a terraform script to create ResourceGroup, VirtualNetwork and Network Security Group)

Process:
Terraform-work(folder): create main.tf (Copt paste code)
run cmds (terraform cmds)

main.tf

terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "3.91.0"
    }
  }
}

provider "azurerm" {
  features {}
}

resource "azurerm_resource_group" "YasCreation" {
  name     = "YasCreation"
  location = "West Europe"
}

resource "azurerm_network_security_group" "snet" {
  name                = "snet"
  location            = azurerm_resource_group.YasCreation.location
  resource_group_name = azurerm_resource_group.YasCreation.name
}

resource "azurerm_virtual_network" "example" {
  name                = "VNET"
  location            = azurerm_resource_group.YasCreation.location
  resource_group_name = azurerm_resource_group.YasCreation.name
  address_space       = ["10.0.0.0/16"]
  dns_servers         = ["10.0.0.4", "10.0.0.5"]

  subnet {
    name           = "subnet1"
    address_prefix = "10.0.1.0/24"
  }

  subnet {
    name           = "subnet2"
    address_prefix = "10.0.2.0/24"
    security_group = azurerm_network_security_group.snet.id
  }

  tags = {
    environment = "Production"
  }
}

resource "azurerm_network_security_group" "SGRoup" {
  name                = "SGRoup"
  location            = azurerm_resource_group.YasCreation.location
  resource_group_name = azurerm_resource_group.YasCreation.name

  security_rule {
    name                       = "test123"
    priority                   = 100
    direction                  = "Inbound"
    access                     = "Allow"
    protocol                   = "Tcp"
    source_port_range          = "*"
    destination_port_range     = "*"
    source_address_prefix      = "*"
    destination_address_prefix = "*"
  }

  tags = {
    environment = "Production"
  }
}

resource "azurerm_resource_group" "example" {
  name     = "example-resources"
  location = "West Europe"
}

resource "azurerm_storage_account" "example" {
  name                     = "yasdb"
  resource_group_name      = azurerm_resource_group.YasCreation.name
  location                 = azurerm_resource_group.YasCreation.location
  account_tier             = "Standard"
  account_replication_type = "LRS"
  account_kind             = "StorageV2"
  is_hns_enabled           = "true"
}

resource "azurerm_storage_data_lake_gen2_filesystem" "example" {
  name               = "example"
  storage_account_id = azurerm_storage_account.example.id

  properties = {
    hello = "aGVsbG8="
  }
}
 


